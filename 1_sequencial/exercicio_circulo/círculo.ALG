Algoritmo "Cálculo da Área do Círculo"

Var
   raio, area: real

Inicio
   // Entrada de dados
   escreva("Digite o valor do raio do círculo: ")
   leia(raio)

   // Cálculo da área
   area <- Pi * Exp(raio, 2)

   // Exibição do resultado
   escreval("Área do círculo = ", area:6:3)

Fimalgoritmo
